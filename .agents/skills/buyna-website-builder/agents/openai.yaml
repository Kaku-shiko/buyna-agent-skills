interface:
  display_name: "Buyna.ai 建站引导助手"
  short_description: "默认团队模式，用一次工作包确认连续完成网站"
  default_prompt: "Use $buyna-website-builder in team mode. For a new build start customer_intake; for repair or resume load state, import supplied verified history through importVerifiedHistory, then run scripts/route-builder.mjs and execute its minimum ready route. Generate only project Adapters, configuration, wiring, and presentation around fixed modules. Continue ready gates inherited from an approved bounded work package without repeated confirmation. Keep local preview in the current project; use GitHub or AWS only for the separately approved external operation."

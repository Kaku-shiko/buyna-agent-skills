function fail(code){throw new Error(code)}function required(value,code){const text=String(value??'').trim();if(!text)fail(code);return text}
export function createIntegrationReceipt({projectId,sellerId,resourceEvidenceId,foundationChecks,expiresAt,now=new Date().toISOString()}={}){for(const key of ['identity','database','storage','authorization'])if(foundationChecks?.[key]!=='PASS')fail(`FOUNDATION_${key.toUpperCase()}_FAILED`);return{schemaVersion:1,projectId:required(projectId,'PROJECT_ID_REQUIRED'),sellerId:required(sellerId,'SELLER_ID_REQUIRED'),resourceEvidenceId:required(resourceEvidenceId,'RESOURCE_EVIDENCE_REQUIRED'),foundationChecks:structuredClone(foundationChecks),completedSlices:[],createdAt:now,updatedAt:now,expiresAt:required(expiresAt,'EXPIRES_AT_REQUIRED')}}
export function addIntegrationSlice({receipt,slice,verification,files=[],now=new Date().toISOString()}={}){if(Date.parse(now)>Date.parse(receipt.expiresAt))fail('INTEGRATION_RECEIPT_EXPIRED');if(!verification?.length||verification.some(item=>item.status!=='PASS'))fail('SLICE_VERIFICATION_FAILED');const next=structuredClone(receipt),key=required(slice,'SLICE_REQUIRED');next.completedSlices=next.completedSlices.filter(item=>item.slice!==key);next.completedSlices.push({slice:key,verification:structuredClone(verification),files:[...files],completedAt:now});next.updatedAt=now;return next}
export function requireIntegrationReceipt({receipt,resourceEvidenceId,now=new Date().toISOString()}={}){if(Date.parse(now)>Date.parse(receipt?.expiresAt))return{status:'blocked',code:'INTEGRATION_RECEIPT_EXPIRED'};if(receipt?.resourceEvidenceId!==resourceEvidenceId)return{status:'blocked',code:'RESOURCE_EVIDENCE_CHANGED'};return{status:'pass',code:'FOUNDATION_RECEIPT_REUSABLE',completedSlices:receipt.completedSlices.map(item=>item.slice)}}

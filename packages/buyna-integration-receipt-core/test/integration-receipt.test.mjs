import test from 'node:test';import assert from 'node:assert/strict';import {addIntegrationSlice,createIntegrationReceipt,requireIntegrationReceipt} from '../src/index.mjs';
const receipt=createIntegrationReceipt({projectId:'project_alpha',sellerId:'seller_alpha',resourceEvidenceId:'evidence-1',foundationChecks:{identity:'PASS',database:'PASS',storage:'PASS',authorization:'PASS'},now:'2026-08-18T00:00:00Z',expiresAt:'2026-08-19T00:00:00Z'});
test('reuses foundation across slices',()=>{const next=addIntegrationSlice({receipt,slice:'products',verification:[{name:'crud',status:'PASS'}],now:'2026-08-18T01:00:00Z'});assert.equal(requireIntegrationReceipt({receipt:next,resourceEvidenceId:'evidence-1',now:'2026-08-18T02:00:00Z'}).status,'pass')});
test('blocks changed or expired evidence',()=>{assert.equal(requireIntegrationReceipt({receipt,resourceEvidenceId:'evidence-2',now:'2026-08-18T02:00:00Z'}).status,'blocked');assert.equal(requireIntegrationReceipt({receipt,resourceEvidenceId:'evidence-1',now:'2026-08-20T00:00:00Z'}).code,'INTEGRATION_RECEIPT_EXPIRED')});

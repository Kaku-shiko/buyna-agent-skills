# Approved Stack Fallback

Preserve a working stack. Only when none exists, select one backend compatible with the approved frontend and record runtime versions, API boundary, database/ORM, migration tool, S3 adapter, authentication, environments, secrets boundary, and build/start/test/release commands. Do not require Django or silently introduce Lovable or Supabase. Save executable configuration and commands in the real project; a recommendation alone is not delivery.

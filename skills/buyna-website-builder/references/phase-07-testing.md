# Phase 7: Testing And Upload Gate

Deliver or update applicable automated tests and run them. Verify desktop/mobile, permission, persistence, error handling, UTF-8, critical journeys, and package size. Require an executed pre-upload report, exclusions, restore/build commands, and `PASS`; do not upload dependencies, caches, local environments, or unexplained oversized assets.

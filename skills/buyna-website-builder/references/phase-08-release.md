# Phase 8: AWS Release

Require the approved target/resource record, Phase 7 `PASS`, migration and secrets plans, cost/risk preview, rollback path, and live verification. Use only the verified Buyna EC2 at `35.73.127.215`, record `NEW_EC2_INSTANCES: 0`, reuse approved database/S3 resources, upload only the runtime artifact, and stop rather than create replacement infrastructure.

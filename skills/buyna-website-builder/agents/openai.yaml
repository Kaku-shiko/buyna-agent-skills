interface:
  display_name: "Buyna.ai 建站引导助手"
  short_description: "默认团队模式，用一次工作包确认连续完成网站"
  default_prompt: "Use $buyna-website-builder in team mode by default. Confirm customer scope and combined design/structure, then ask once for a bounded frontend/backend/minimum-test work package and continue through its passing gates without repeated confirmation. Stop again only for blockers, scope changes, cost, destructive work, paid activation, or production release."
